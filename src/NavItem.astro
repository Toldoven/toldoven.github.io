---

interface Props {
    label: string;
    href?: string;    
}

const { label, href } = Astro.props;

---

<a {href} aria-label={label} class="flex px-5 py-3 rounded-full text-lg font-semibold bg-blue-600 hover:brightness-105 transition">{label}</a>